# Gallery

Output renders go here: shader screenshots/recordings, datamosh'd video
exports, notebook-generated visualizations. Not tracked by git by default
(see `.gitignore`) except for this file — drop your own renders in
locally, or commit specific ones you want to share by adding them
explicitly with `git add -f`.
